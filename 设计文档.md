# UGT.UniTween 动画插件设计文档

## 一、背景与目标

- **目标**：提供一个双模驱动的 Unity 动画插件包，既支持纯代码 API 快速编写动画（对标 DOTween），也支持通过可视化组件在 Inspector 中编排动画序列，无需编写任何代码即可完成常见 UI/Transform 动画。
- **交付形态**：Unity Package（UPM），独立存放于 `d:\Projects\UGF\UGT.UniTween`。
- **适用场景**：UI 动效、物件入场/退场、过场动画、数值/颜色/材质的渐进变化等。

## 二、核心设计理念

```
                     ┌──────────────────────────┐
                     │      UGT.UniTween        │
                     └──────────────────────────┘
                                 │
              ┌──────────────────┴──────────────────┐
              │                                     │
     ┌────────▼────────┐                  ┌─────────▼─────────┐
     │   Code API 模式   │                  │   Component 模式    │
     │  (类似 DOTween)   │                  │ (TweenPlayer 组件)  │
     └────────┬────────┘                  └─────────┬─────────┘
              │                                     │
              └──────────────────┬──────────────────┘
                                 │
                    ┌────────────▼────────────┐
                    │      TweenEngine        │
                    │  (统一的插值/缓动/调度)    │
                    └─────────────────────────┘
```

两种使用模式共享同一个底层引擎（TweenEngine），只是暴露方式不同：

- **Code API**：通过链式调用快速创建动画，适合程序化动态逻辑。
- **Component**：通过挂载 `TweenPlayer` 组件，在 Inspector 中以序列化方式编排动画剪辑（TweenClip），适合设计师与静态固定动画。

## 三、功能特性

### V1 核心功能

- **双模驱动**：同一底层引擎，支持代码 API 与可视化组件两种使用方式。
- **丰富的属性动画**：Transform（位置/旋转/缩放）、RectTransform（锚点/大小/偏移）、CanvasGroup（透明度）、Image/Text（颜色/透明度）、SpriteRenderer（颜色/透明度/Flip/Size）、数值插值。
- **完整缓动库**：内置 30+ 缓动函数（EaseIn/Out/InOut × Sine/Quad/Cubic/Quart/Quint/Expo/Circ/Back/Elastic/Bounce + Linear）。
- **序列编排**：支持链式组合多个 Tween，顺序执行、延迟、回调、循环。
- **可视化编辑**：TweenPlayer 组件内嵌动画轨道（Track）与剪辑（Clip）的概念，支持多轨道并行，轨道内串行。
- **编辑器预览**：在 Edit Mode 下即可预览动画序列。
- **对象池管理**：Tween 实例复用，减少 GC 分配。

### 后续规划（V2+）

- 自定义缓动曲线编辑器
- 路径动画（Path Tween）
- Punch/Shake 特效动画
- 材质属性动画（MaterialPropertyBlock）
- DOTween 迁移适配层
- Timeline 轨道集成

## 四、技术架构

### 4.1 模块划分

```
UGT.UniTween/
├── Runtime/
│   ├── Core/
│   │   ├── TweenEngine.cs           # 核心引擎：调度、更新、池管理
│   │   ├── Tween.cs                 # 单个 Tween 实例（可独立或组成序列）
│   │   ├── TweenSequence.cs         # 顺序/并行序列
│   │   ├── EaseCurves.cs            # 缓动函数库
│   │   ├── TweenSettings.cs         # 全局配置（默认缓动、时间缩放等）
│   │   └── TweenObjectPool.cs       # Tween 对象池
│   ├── Plugins/
│   │   ├── TransformPlugins.cs      # Transform 相关动画
│   │   ├── RectTransformPlugins.cs  # RectTransform 相关动画
│   │   ├── UGUIPlugins.cs           # UGUI 组件动画（Image/Text/CanvasGroup）
│   │   ├── SpriteRendererPlugins.cs  # SpriteRenderer 2D 物体动画
│   │   └── ValuePlugins.cs          # 纯数值插值（float/int/Vector/Color）
│   ├── Components/
│   │   ├── TweenPlayer.cs           # 动画序列播放组件（核心组件）
│   │   ├── TweenTrack.cs           # 动画轨道（一组串行 Clip）
│   │   ├── TweenClip.cs            # 单段动画剪辑（数据定义）
│   │   └── TweenEvent.cs           # 序列回调事件定义
│   ├── Extensions/
│   │   ├── TweenExtensions.cs       # Code API 扩展方法（如 transform.DoMoveX()）
│   │   └── SequenceExtensions.cs    # 序列链式扩展方法
│   └── UGT.UniTween.asmdef
├── Editor/
│   ├── Components/
│   │   ├── TweenPlayerEditor.cs     # TweenPlayer 自定义 Inspector
│   │   ├── TweenTrackEditor.cs     # 轨道编辑器
│   │   ├── TweenClipDrawer.cs      # Clip 属性绘制器
│   │   └── EaseCurveDrawer.cs      # 缓动曲线预览绘制器
│   ├── Windows/
│   │   └── TweenTimelineWindow.cs   # 可视化时间轴编辑器窗口
│   ├── Utilities/
│   │   ├── TweenPreviewer.cs        # Edit Mode 动画预览
│   │   └── TweenEditorSettings.cs   # 编辑器偏好设置
│   └── UGT.UniTween.Editor.asmdef
├── Samples~/
│   ├── BasicTween/
│   │   ├── Scripts/
│   │   │   └── BasicTweenDemo.cs
│   │   ├── Scenes/
│   │   │   └── BasicTween.unity
│   │   └── README.md
│   ├── SequenceDemo/
│   │   ├── Scripts/
│   │   ├── Scenes/
│   │   └── README.md
│   └── TweenPlayerDemo/
│       ├── Prefabs/
│       ├── Scenes/
│       └── README.md
├── package.json
└── 设计文档.md
```

### 4.2 核心类设计

#### TweenEngine（核心引擎）

```csharp
namespace UGT.UniTween.Core
{
    /// <summary>
    /// Tween 调度引擎。场景单例，每帧 Update 驱动所有活跃 Tween。
    /// </summary>
    public sealed class TweenEngine : MonoBehaviour
    {
        public static TweenEngine Instance { get; }

        // 全局时间缩放（独立于 Time.timeScale）
        public float TimeScale { get; set; }

        // 活跃 Tween 数量
        public int ActiveTweenCount { get; }

        // 注册/注销 Tween（内部使用，用户不直接调用）
        internal void Register(Tween tween);
        internal void Unregister(Tween tween);

        // 销毁所有活跃 Tween
        public void KillAll();
    }
}
```

#### Tween（原子动画）

```csharp
namespace UGT.UniTween.Core
{
    /// <summary>
    /// 单段 Tween 实例。封装插值逻辑、缓动、回调。
    /// 支持链式组合为序列。
    /// </summary>
    public sealed class Tween
    {
        public float Duration { get; set; }       // 持续时间
        public float Delay { get; set; }          // 延迟时间
        public int Loops { get; set; }            // 循环次数（-1 = 无限）
        public LoopType LoopType { get; set; }    // Restart / Yoyo
        public EaseType Ease { get; set; }         // 缓动类型

        // 插值更新回调（驱动具体属性动画）
        public Action<float> OnUpdate { get; set; }
        // 生命周期回调
        public Action OnStart { get; set; }
        public Action OnComplete { get; set; }
        public Action OnKill { get; set; }

        // 控制
        public Tween Play();
        public Tween Pause();
        public Tween Resume();
        public void Kill();
        public void Complete();      // 立即跳到终点

        // 链式
        public Tween Append(Tween next);          // 追加（串行）
        public Tween Join(Tween parallel);        // 加入并行
        public TweenSequence ToSequence();        // 转为序列对象
    }
}
```

#### TweenSequence（序列）

```csharp
namespace UGT.UniTween.Core
{
    /// <summary>
    /// Tween 序列。管理多个 Tween 的串行/并行编排。
    /// </summary>
    public sealed class TweenSequence
    {
        public TweenSequence Append(Tween tween);
        public TweenSequence AppendInterval(float duration);
        public TweenSequence AppendCallback(Action callback);
        public TweenSequence Join(Tween tween);          // 与上一个并行

        public TweenSequence Insert(float atPosition, Tween tween);  // 在指定位置插入

        // 控制
        public TweenSequence Play();
        public void Pause();
        public void Resume();
        public void Kill();
        public void Complete();
    }
}
```

#### 扩展方法（Code API）

```csharp
// 使用示例
transform.DoMoveX(5f, 1.5f)
    .SetEase(EaseType.OutBack)
    .SetDelay(0.3f)
    .SetLoops(2, LoopType.Yoyo)
    .OnComplete(() => Debug.Log("Done"));

image.DoFade(0f, 0.5f)
    .SetEase(EaseType.InQuad);

// 序列示例
var sequence = UniTween.Sequence();
sequence.Append(transform.DoMoveX(5f, 1f))
    .AppendInterval(0.2f)
    .Append(transform.DoScale(Vector3.one * 1.5f, 0.5f))
    .AppendCallback(() => Debug.Log("Sequence done"))
    .Play();
```

### 4.3 Component 模式核心类

#### TweenPlayer（动画播放组件）

```csharp
namespace UGT.UniTween.Components
{
    /// <summary>
    /// 动画序列播放组件。
    /// 挂载到目标 GameObject 上，通过 Inspector 编辑动画序列。
    /// </summary>
    public sealed class TweenPlayer : MonoBehaviour
    {
        // 播放控制
        public PlayMode PlayOnEnable = PlayMode.None;  // None/Play/Restart
        public UpdateMode UpdateType = UpdateMode.Normal; // Normal/Late/Fixed/Unscaled

        // 轨道列表（每条轨道并行执行，轨道内 Clip 串行）
        public List<TweenTrack> Tracks;

        // 运行时控制
        public bool IsPlaying { get; }
        public void Play();
        public void Pause();
        public void Resume();
        public void Stop();
        public void Restart();
        public void Seek(float normalizedTime);

        // 事件
        public UnityEvent OnPlay;
        public UnityEvent OnComplete;
    }
}
```

#### TweenTrack（动画轨道）

```csharp
namespace UGT.UniTween.Components
{
    /// <summary>
    /// 一条动画轨道。包含一组串行执行的 TweenClip。
    /// 多条轨道在 TweenPlayer 下并行执行。
    /// </summary>
    [Serializable]
    public sealed class TweenTrack
    {
        public string TrackName;
        public GameObject Target;                  // 目标对象

        public List<TweenClip> Clips;              // 串行剪辑列表
    }
}
```

#### TweenClip（动画剪辑）

```csharp
namespace UGT.UniTween.Components
{
    /// <summary>
    /// 单段动画剪辑的数据定义。描述对某个属性的一段插值动画。
    /// </summary>
    [Serializable]
    public sealed class TweenClip
    {
        public string ClipName;
        public float Duration = 0.5f;
        public float Delay;
        public EaseType Ease = EaseType.OutQuad;
        public int Loops;
        public LoopType LoopType;

        public TweenTargetType TargetType;          // Transform / RectTransform / CanvasGroup / Image / Text / SpriteRenderer
        public TweenPropertyType PropertyType;      // Position / Rotation / Scale / Alpha / Color / ...

        // 起始值与结束值（支持相对/绝对模式）
        public TweenValueMode ValueMode;            // Absolute / Relative
        public Vector3 FromValue;
        public Vector3 ToValue;
    }
}
```

### 4.4 枚举定义

```csharp
namespace UGT.UniTween
{
    public enum EaseType
    {
        Linear,
        InSine, OutSine, InOutSine,
        InQuad, OutQuad, InOutQuad,
        InCubic, OutCubic, InOutCubic,
        InQuart, OutQuart, InOutQuart,
        InQuint, OutQuint, InOutQuint,
        InExpo, OutExpo, InOutExpo,
        InCirc, OutCirc, InOutCirc,
        InBack, OutBack, InOutBack,
        InElastic, OutElastic, InOutElastic,
        InBounce, OutBounce, InOutBounce,
    }

    public enum LoopType { Restart, Yoyo }

    public enum PlayMode { None, Play, Restart }

    public enum UpdateMode { Normal, Late, Fixed, Unscaled }

    public enum TweenValueMode { Absolute, Relative }

    public enum TweenTargetType { Transform, RectTransform, CanvasGroup, Image, Text, SpriteRenderer, Material }

    public enum TweenPropertyType
    {
        // Transform
        Position, PositionX, PositionY, PositionZ,
        Rotation, RotationX, RotationY, RotationZ,
        Scale, ScaleX, ScaleY, ScaleZ,
        // RectTransform
        AnchorPosition, AnchorMin, AnchorMax,
        SizeDelta, Pivot,
        // CanvasGroup
        Alpha,
        // Image / Text
        Color, Fade,
        // SpriteRenderer
        SpriteColor, SpriteAlpha, FlipX, FlipY, SpriteSize,
        // Float
        FloatValue,
    }
}
```

## 五、数据流与生命周期

### 5.1 Code API 流程

```
用户代码
    │
    ▼
扩展方法（transform.DoMoveX(...)）
    │
    ▼
TweenEngine.GetTween() ──► 从对象池获取 Tween 实例
    │
    ▼
Tween.Play() ──► TweenEngine.Register()
    │
    ▼
TweenEngine.Update() ──► 每帧遍历活跃 Tween
    │                     ├─ 检查 Delay
    │                     ├─ 调用 EaseCurves.Evaluate() 计算插值系数 t
    │                     ├─ 调用 tween.OnUpdate(t) 驱动属性变化
    │                     └─ 到达终点 → 处理 Loops → 触发 OnComplete → 回收
    │
    ▼
Tween.Kill() / Complete() ──► TweenEngine.Unregister() → 回池
```

### 5.2 Component 流程

```
TweenPlayer (挂载在 GameObject 上)
    │
    ▼
Play() / PlayOnEnable ──► 遍历 Tracks
    │
    ▼
每条 Track ──► 遍历 Clips（串行）
    │
    ▼
每个 Clip ──► 据 TweenTargetType + TweenPropertyType
    │           创建对应的 Tween 实例（内部走 Code API 相同路径）
    │
    ▼
所有 Track 并行推进（通过 TweenSequence.Join）
    │
    ▼
所有完成 → 触发 TweenPlayer.OnComplete
```

### 5.3 生命周期

```
                  Kill()
              ┌───────────┐
              │           │
   Created ──► Waiting ──► Playing ──► Completed ──► Recycled
              │ (delay)     ▲   │                     ▲
              │             │   │                     │
              │        Resume │   └─ Loops > 0 ───────┘ (Restart/Yoyo)
              │             │
              └── Pause ────┘
```

## 六、Visual Timeline Editor 设计（V1）

### 6.1 概述

为 `TweenPlayer` 提供一个可视化的时间轴编辑器窗口，可直观编排动画序列。

### 6.2 窗口布局

```
┌──────────────────────────────────────────────────────────────┐
│  Tween Timeline: [MyAnimation]    [▶] [⏸] [⏹] [⟲]          │
├──────┬───────────────────────────────────────────────────────┤
│      │ 0.0    0.5    1.0    1.5    2.0    2.5    3.0        │
├──────┼───────────────────────────────────────────────────────┤
│ Trk1 │ ╔══════════╗     ╔══════════╗                        │
│ Pos  │ ║ MoveIn   ║     ║ MoveOut  ║                        │
├──────┼─╚══════════╝─────╚══════════╝────────────────────────┤
│ Trk2 │           ╔═════════════════╗                         │
│ Scl  │           ║  ScaleBounce   ║                         │
├──────┼───────────╚═════════════════╝────────────────────────┤
│ Trk3 │ ╔══════════════════════════════════╗                  │
│ Clr  │ ║         ColorFade               ║                  │
└──────┴─╚══════════════════════════════════╝──────────────────┘
        ↑ 时间标尺                        ↑ 播放头

左侧面板：轨道列表（增删、重命名、锁定、折叠）
右侧面板：时间轴区域（拖拽 Clip 调整时间/时长、多选、缩放）
底部面板：Clip 属性面板（选中 Clip 后显示 Duration/Ease/From/To 等详情）
```

### 6.3 交互功能

- **Clip 拖拽**：水平拖动调整起始时间，拖拽边缘调整 Duration。
- **播放头**：可拖动 Seek 到任意位置；选中 Clip 时自动吸附。
- **缩放**：滚轮缩放时间轴，适配不同长度的动画。
- **预览**：Edit Mode 下播放可在 Scene View 中实时预览。
- **多选**：Shift/Ctrl 多选 Clip 进行批量编辑。
- **右键菜单**：添加轨道、添加 Clip、复制/粘贴 Clip、删除、设置缓动。

## 七、API 使用示例

### 7.1 Code API 基础用法

```csharp
using UGT.UniTween;
using UGT.UniTween.Extensions;

// 单段动画
transform.DoMove(new Vector3(5, 0, 0), 1.2f)
    .SetEase(EaseType.OutBack);

// 链式配置
GetComponent<CanvasGroup>().DoFade(1f, 0.3f)
    .SetDelay(0.5f)
    .SetLoops(3, LoopType.Yoyo)
    .OnComplete(() => Debug.Log("Fade done"));

// 序列编排
var seq = UniTween.CreateSequence();
seq.Append(transform.DoMoveX(3f, 0.5f))
   .Join(transform.DoScale(Vector3.one * 1.2f, 0.5f))
   .AppendInterval(0.3f)
   .Append(transform.DoMoveX(0f, 0.5f))
   .AppendCallback(() => gameObject.SetActive(false))
   .Play();

// 纯数值动画
float health = 100f;
UniTween.DoFloat(100f, 0f, 0.5f, v => health = v)
    .SetEase(EaseType.OutCubic);

// 颜色过渡
image.DoColor(Color.red, 0.4f).SetEase(EaseType.InOutSine);

// SpriteRenderer 2D 动画
spriteRenderer.DoColor(Color.blue, 1f).SetEase(EaseType.OutSine);
spriteRenderer.DoFade(0.3f, 0.8f).SetLoops(-1, LoopType.Yoyo);
spriteRenderer.DoFlipX(true, 0.3f);
```

### 7.2 Component 模式基础用法

1. 选择目标 `GameObject`，挂载 `TweenPlayer` 组件。
2. 在 Inspector 中添加 `TweenTrack`。
3. 为每条 Track 设置目标对象（如自身或其他对象）和类型。
4. 在 Track 下添加 `TweenClip`，配置属性类型、起始/结束值、时长、缓动。
5. 在 `TweenPlayer` 中设置 `PlayOnEnable` 或通过外部事件调用 `Play()`。

## 八、性能设计

- **对象池**：Tween 实例从池中获取和归还，避免频繁 New/GC。
- **更新优化**：TweenEngine 仅遍历活跃 Tween；Pause 状态的 Tween 跳过插值运算但保持注册。
- **惰性初始化**：TweenEngine 仅在首个 Tween 创建时自动实例化。
- **批量控制**：TweenPlayer 内部对同一目标的多个 Tween 合批提交。
- **值类型优化**：缓动计算使用 Struct 封装的 `EaseCurve`，避免虚调用。

## 九、编辑器工具与调试

- **TweenTracker**：Runtime 下显示当前活跃的 Tween 列表及状态（可选 Debug 模块）。
- **Hot Reload 安全**：Domain Reload 后自动清理残留 Tween 引用。
- **编辑器预览**：`TweenPreviewer` 支持在 Edit Mode 下驱动 TweenPlayer 预览，便于调参。

## 十、兼容性要求

- Unity 2020.3 LTS 或更高版本。
- 依赖：UnityEngine.CoreModule、UnityEngine.UIModule、UnityEngine.AnimationModule。
- 无其它第三方运行时依赖。

## 十一、版本规划

| 版本 | 内容 |
|------|------|
| v0.1 | Core 引擎（插值/缓动/序列）、Code API 扩展方法、TweenObjectPool |
| v0.2 | TweenPlayer / TweenTrack / TweenClip 组件、Inspector 编辑器 |
| v0.3 | Timeline Window 可视化编辑器 |
| v0.4 | Punch/Shake 特效、Material 属性动画、自定义缓动曲线 |
| v0.5 | DOTween 迁移适配层、Timeline Integration |

## 十二、命名规范

- 命名空间：`UGT.UniTween`（Core）、`UGT.UniTween.Extensions`（扩展方法）、`UGT.UniTween.Components`（组件）。
- 类命名：PascalCase；私有字段 `_camelCase`。
- 扩展方法命名：`Do{Property}` 风格（如 `DoMove`、`DoFade`、`DoColor`）。
- Ease 曲线函数：`Evaluate` 方法接收归一化时间 (0..1)，返回归一化进度 (0..1)。

## 十三、风险与对策

| 风险 | 对策 |
|------|------|
| 对象池泄漏（Tween 未正确回收） | Timeout 自动回收机制 + Debug 面板监控 |
| Component 模式下序列化数据膨胀 | Clip 数据使用紧凑结构，减少冗余字段 |
| 编辑器与 Runtime 行为不一致 | 编辑器预览直接调用 Runtime 路径，保证一致性 |
| 与 Unity Animator/Animation 冲突 | 文档明确说明互斥场景；提供 Safe Mode 检测 |
| Timeline 编辑器 UI 复杂度高 | 分阶段交付，V1 仅实现基础拖拽与预览 |
