# UGT.UniTween 使用文档

## 目录

1. [快速开始](#1-快速开始)
2. [Code API 模式](#2-code-api-模式)
3. [Component 模式](#3-component-模式)
4. [TweenPlayerSpawner 批量生成器](#4-tweenplayerspawner-批量生成器)
5. [SpriteRendererGroup 组件](#5-spriterenderergroup-组件)
6. [编辑模式预览](#6-编辑模式预览)
7. [全局配置](#7-全局配置)
8. [API 参考](#8-api-参考)

---

## 1. 快速开始

### 安装

将包文件夹 `UGT.UniTween/` 放入项目的 `Packages/` 目录，或通过 Package Manager 以本地包添加。

### 最小示例（Code API）

```csharp
using UGT.UniTween;
using UGT.UniTween.Plugins;

// 一行代码播放移动动画
transform.DoMove(new Vector3(5, 0, 0), 1.2f)
    .SetEase(EaseType.OutBack);
```

### 最小示例（Component）

1. 在任意 `GameObject` 上添加 `TweenPlayer` 组件（菜单：`Component > UGT UniTween > Tween Player`）
2. 在 Inspector 中添加 Track 和 Clip
3. 设置 `Play On Enable` 为 `Play`，或运行时调用 `Play()`

---

## 2. Code API 模式

### 2.1 Transform 动画

```csharp
// 世界坐标移动
transform.DoMove(new Vector3(5, 0, 0), 1.2f);
transform.DoMoveX(3f, 0.5f);       // 仅在 X 轴移动
transform.DoMoveY(2f, 0.8f);       // 仅在 Y 轴移动
transform.DoMoveZ(-1f, 0.3f);      // 仅在 Z 轴移动

// 本地坐标移动
transform.DoLocalMove(new Vector3(0, 2, 0), 1f);
transform.DoLocalMoveX(3f, 0.5f);
transform.DoLocalMoveY(2f, 0.5f);
transform.DoLocalMoveZ(-1f, 0.3f);

// 旋转（欧拉角）
transform.DoRotate(new Vector3(0, 90, 0), 1f);

// 旋转（四元数）
transform.DoRotateQuaternion(Quaternion.Euler(0, 90, 0), 1f);

// 缩放
transform.DoScale(Vector3.one * 2f, 1f);   // 统一缩放
transform.DoScale(2f, 1f);                  // 快捷写法
transform.DoScaleX(1.5f, 0.5f);             // 仅 X 轴
transform.DoScaleY(1.5f, 0.5f);             // 仅 Y 轴
transform.DoScaleZ(1.5f, 0.5f);             // 仅 Z 轴
```

### 2.2 RectTransform 动画

```csharp
RectTransform rt = GetComponent<RectTransform>();

rt.DoAnchorPos(new Vector2(100, 0), 0.5f);
rt.DoAnchorPosX(100f, 0.3f);
rt.DoAnchorPosY(50f, 0.3f);
rt.DoAnchorMin(new Vector2(0, 0), 0.5f);
rt.DoAnchorMax(new Vector2(1, 1), 0.5f);
rt.DoSizeDelta(new Vector2(200, 100), 0.5f);
rt.DoPivot(new Vector2(0.5f, 0.5f), 0.3f);
```

### 2.3 UGUI 动画

```csharp
// CanvasGroup
canvasGroup.DoFade(0f, 0.5f);     // 淡出
canvasGroup.DoAlpha(1f, 0.3f);    // 淡入（与 DoFade 等价）

// Image
Image image = GetComponent<Image>();
image.DoColor(Color.red, 0.4f);          // 颜色过渡
image.DoFade(0.5f, 0.3f);               // 仅透明度
image.DoFillAmount(0.8f, 0.5f);         // 填充量

// Text (Legacy)
Text text = GetComponent<Text>();
text.DoColor(Color.green, 0.5f);
text.DoFade(0f, 0.3f);
```

### 2.4 SpriteRenderer 2D 动画

```csharp
SpriteRenderer sr = GetComponent<SpriteRenderer>();

sr.DoColor(Color.blue, 1f);           // 颜色过渡
sr.DoFade(0.3f, 0.8f);               // 透明度过渡
sr.DoAlpha(0f, 0.5f);                // 同上
sr.DoFlipX(true, 0.3f);              // 水平翻转
sr.DoFlipY(false, 0.3f);             // 垂直翻转
sr.DoSize(new Vector2(2, 2), 1f);     // 尺寸变化
```

### 2.5 纯数值与颜色动画

```csharp
// 浮点插值
UniTween.DoFloat(100f, 0f, 0.5f, v => healthBar.fillAmount = v)
    .SetEase(EaseType.OutCubic);

// 颜色插值
UniTween.DoColor(Color.white, Color.black, 1f, c => renderer.material.color = c);

// Vector2 / Vector3 插值
UniTween.DoVector3(Vector3.zero, Vector3.one, 2f, v => someVar = v);
```

### 2.6 链式配置

所有 Tween 都支持以下链式 API：

```csharp
transform.DoMoveX(5f, 1.5f)
    .SetEase(EaseType.OutBack)           // 缓动类型
    .SetDuration(1f)                      // 重设时长
    .SetDelay(0.3f)                       // 延迟播放
    .SetLoops(3, LoopType.Yoyo)           // 循环 3 次 + 来回
    .SetOnStart(() => Debug.Log("开始"))
    .SetOnComplete(() => Debug.Log("完成"))
    .SetOnKill(() => Debug.Log("被终止"))
    .SetOnUpdate(t => { /* t 为归一化进度 0..1 */ });

// 无限循环（Loops = -1 或 LoopType.Yoyo）
transform.DoScale(Vector3.one * 1.2f, 0.5f)
    .SetLoops(-1, LoopType.Yoyo);

// 自定义动画曲线（替代预设 EaseType）
transform.DoMoveY(3f, 1f)
    .SetEase(myAnimationCurve);
```

### 2.7 序列（Sequence）

```csharp
var seq = UniTween.CreateSequence();

seq.Append(transform.DoMoveX(3f, 0.5f))             // 串行 1
   .Join(transform.DoScale(1.5f, 0.5f))              // 与上一步并行
   .AppendInterval(0.3f)                              // 等待 0.3 秒
   .Append(transform.DoMoveX(0f, 0.5f))              // 串行 2
   .AppendCallback(() => gameObject.SetActive(false)) // 回调
   .Play();

// Sequence 也支持 OnComplete
seq.OnComplete = () => Debug.Log("序列完成");
```

### 2.8 控制方法

```csharp
Tween tween = transform.DoMoveX(5f, 2f);

tween.Play();      // 播放
tween.Pause();     // 暂停
tween.Resume();    // 恢复
tween.Kill();      // 终止（不触发 OnComplete）
tween.Complete();  // 立即跳到终点（触发 OnComplete）
```

### 2.9 全局控制

```csharp
UniTween.KillAll();                      // 销毁所有活跃动画
UniTween.TimeScale = 0.5f;               // 全局变速
Debug.Log(UniTween.ActiveTweenCount);    // 查看活跃 Tween 数量
Debug.Log(UniTween.ActiveSequenceCount); // 查看活跃序列数量
```

---

## 3. Component 模式

### 3.1 TweenPlayer 组件

`TweenPlayer` 是可视化编排动画序列的核心组件，挂载到 GameObject 上使用。

#### 组件属性

| 属性 | 说明 |
|------|------|
| Play On Enable | `None`：不自动播放；`Play`：Enable 时播放（不重置）；`Restart`：Enable 时从头播放 |
| Update Type | 驱动模式：`Normal`/`Late`/`Fixed`/`Unscaled` |
| Tracks | 动画轨道列表（多条轨道并行，单条轨道内 Clip 串行） |
| Events | 序列事件列表（在指定进度百分比触发 UnityEvent） |
| On Play | 播放开始时的 UnityEvent |
| On Complete | 播放完毕时的 UnityEvent |

#### 轨道（Track）

| 属性 | 说明 |
|------|------|
| Track Name | 轨道名称 |
| Target | 目标对象（留空则使用 TweenPlayer 所在对象） |
| Clips | 串行动画剪辑列表 |

#### 剪辑（Clip）

| 属性 | 说明 |
|------|------|
| Name | 剪辑名称 |
| Duration | 持续时间（秒） |
| Delay | 延迟时间（秒） |
| Ease | 缓动类型（30+ 种预设 + Custom 曲线） |
| Loops | 循环次数（0=不循环，-1=无限） |
| Loop Type | `Restart`（重播）或 `Yoyo`（来回） |
| Target | 目标组件类型（Transform/Image/SpriteRenderer 等） |
| Property | 属性类型（根据 Target 动态过滤） |
| Value Mode | `Absolute`（绝对）或 `Relative`（相对，Color/Flip 不可用） |
| From | 起始值（可选，使用右侧勾选框启用） |
| To | 结束值 |

#### TargetType 与 PropertyType 对应关系

| TargetType | 可用 PropertyType | ValueMode |
|---|---|---|
| Transform | Position, PositionX/Y/Z, Rotation, Scale, ScaleX/Y/Z | ✅ |
| RectTransform | AnchorPosition, AnchorMin, AnchorMax, SizeDelta, Pivot | ✅ |
| CanvasGroup | Alpha | ✅ |
| Image | Color, Fade | Color 不支持 |
| Text | Color, Fade | Color 不支持 |
| SpriteRenderer | SpriteColor, SpriteAlpha, FlipX, FlipY, SpriteSize | Flip 不支持 |
| SpriteRendererGroup | Alpha | ✅ |

#### 运行时 API

```csharp
TweenPlayer player = GetComponent<TweenPlayer>();

player.Play();           // 播放
player.Pause();          // 暂停
player.Resume();         // 恢复
player.Stop();           // 停止
player.Restart();        // 从头播放
player.Seek(0.5f);      // 跳转到 50% 进度（0~1）
player.IsPlaying;        // 是否正在播放
player.IsPaused;         // 是否暂停中
```

#### Inspector 运行时控制

在 Play Mode 下，TweenPlayer 的 Inspector 顶部会显示控制按钮：`Play`/`Pause`、`Stop`、`Restart`，可直接调试。

### 3.2 使用步骤

1. 选择目标 `GameObject`，菜单 `Component > UGT UniTween > Tween Player`
2. 在 `Tracks` 列表中点击 `+` 添加轨道
3. 设置轨道的 `Target`（可选，留空则操作自身）
4. 在 `Clips` 中点击 `+` 添加剪辑
5. 展开剪辑面板，配置 `Property`（先选 `Target`，再选 `Property`）
6. 设置 `To` 值、`Duration`、`Ease` 等参数
7. 勾选 `Use From` 可配置起始值
8. 设置 `Play On Enable` 为 `Play`，或通过代码调用 `Play()`

---

## 4. TweenPlayerSpawner 批量生成器

`TweenPlayerSpawner` 用于批量生成带 TweenPlayer 动画的预制体实例，支持两种模式。

### 组件属性

| 属性 | 说明 |
|------|------|
| Prefab | 带有 TweenPlayer 组件的预制体 |
| Mode | `None`/`Burst`（一次性爆发）/`Loop`（循环生成） |
| Burst Count | Burst 模式下生成数量 |
| Spawn Interval | Loop 模式下生成间隔（秒） |
| Pool Capacity | Loop 模式对象池初始容量 |
| Spawn As Child | 生成的实例是否作为子节点 |
| Random Offset | 生成位置随机偏移范围 |

### 两种模式

```csharp
// Burst 模式：Start 时一次性生成指定数量
// Loop 模式：Start 时开始循环，按间隔从对象池取出实例播放

TweenPlayerSpawner spawner = GetComponent<TweenPlayerSpawner>();

// 手动控制
spawner.SpawnBurst(10);    // 即时生成 10 个
spawner.StartLoop();       // 开始循环
spawner.StopLoop();        // 停止循环并回收

spawner.IsLooping;         // 是否正在循环
```

### Inspector 预览

在非 Play Mode 下，Inspector 提供 `Spawn Burst` 按钮可直接预览生成效果。

---

## 5. SpriteRendererGroup 组件

`SpriteRendererGroup` 类似 `CanvasGroup`，用于统一控制自身及所有子级 `SpriteRenderer` 的透明度和颜色。

### 组件属性

| 属性 | 说明 |
|------|------|
| Alpha | 统一透明度（0~1） |
| Override Color | 是否覆盖颜色 |
| Color | 统一覆盖颜色 |

### 使用方式

```csharp
SpriteRendererGroup group = GetComponent<SpriteRendererGroup>();

// 运行时控制
group.Alpha = 0.5f;
group.OverrideColor = true;
group.Color = Color.red;

// 手动刷新子级缓存
group.RefreshRenderers();

// 手动应用属性
group.Apply();
```

### 配合 TweenPlayer 使用

TweenPlayer 的 `TargetType` 中支持 `SpriteRendererGroup`，可直接对其 `Alpha` 属性创建动画。

---

## 6. 编辑模式预览

UGT.UniTween 内置编辑器驱动（`EditorTweenDriver`），在非 Play Mode 下也能预览动画：

- **Code API**：在 Edit Mode 下调用 Tween 方法，动画会实时在 Scene View 中播放
- **TweenPlayer**：Inspector 中的 `Spawn Burst` 按钮可即时预览生成效果
- 进入 Play Mode 时会自动清除 Edit Mode 残留动画

---

## 7. 全局配置

通过 `TweenSettings`（ScriptableObject）可配置全局默认项：

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| Default Ease | OutQuad | 默认缓动类型 |
| Max Active Tweens | 512 | 最大活跃 Tween 数 |
| Pool Initial Capacity | 64 | Tween 对象池初始容量 |
| Timeout Seconds | 0 | Tween 超时自动回收（秒，0=禁用） |

---

## 8. API 参考

### 8.1 缓动曲线（EaseType）

共 31 种：

| 类别 | 枚举值 |
|------|--------|
| Linear | `Linear` |
| Sine | `InSine` `OutSine` `InOutSine` |
| Quad | `InQuad` `OutQuad` `InOutQuad` |
| Cubic | `InCubic` `OutCubic` `InOutCubic` |
| Quart | `InQuart` `OutQuart` `InOutQuart` |
| Quint | `InQuint` `OutQuint` `InOutQuint` |
| Expo | `InExpo` `OutExpo` `InOutExpo` |
| Circ | `InCirc` `OutCirc` `InOutCirc` |
| Back | `InBack` `OutBack` `InOutBack` |
| Elastic | `InElastic` `OutElastic` `InOutElastic` |
| Bounce | `InBounce` `OutBounce` `InOutBounce` |
| Custom | `Custom` —— 配合 `AnimationCurve` 使用 |

### 8.2 静态类：UniTween

| 方法 | 说明 |
|------|------|
| `UniTween.CreateSequence()` | 创建空序列 |
| `UniTween.DoFloat(from, to, duration, callback)` | 浮点插值 |
| `UniTween.DoColor(from, to, duration, callback)` | 颜色插值 |
| `UniTween.DoVector3(from, to, duration, callback)` | Vector3 插值 |
| `UniTween.KillAll()` | 销毁所有活跃动画 |
| `UniTween.TimeScale` | 全局时间缩放 |
| `UniTween.ActiveTweenCount` | 当前活跃 Tween 数量 |
| `UniTween.ActiveSequenceCount` | 当前活跃序列数量 |

### 8.3 Tween 实例方法

| 方法 | 说明 |
|------|------|
| `Play()` | 播放（从起点或暂停恢复） |
| `Pause()` | 暂停 |
| `Resume()` | 恢复 |
| `Kill(bool complete)` | 终止（complete=true 时跳到终点） |
| `Complete()` | 立即跳到终点 |
| `SetEase(EaseType)` | 设置缓动 |
| `SetEase(AnimationCurve)` | 设置自定义曲线 |
| `SetDuration(float)` | 设置时长 |
| `SetDelay(float)` | 设置延迟 |
| `SetLoops(int, LoopType)` | 设置循环 |
| `SetOnStart(Action)` | 开始回调 |
| `SetOnComplete(Action)` | 完成回调 |
| `SetOnKill(Action)` | 终止回调 |
| `SetOnUpdate(Action<float>)` | 每帧回调（t 为归一化进度 0..1） |

### 8.4 TweenSequence 方法

| 方法 | 说明 |
|------|------|
| `Append(Tween)` | 追加 Tween（串行） |
| `Append(TweenSequence)` | 追加子序列 |
| `Join(Tween)` | 与上一项并行 |
| `Join(TweenSequence)` | 子序列并行 |
| `AppendInterval(float)` | 追加等待时间 |
| `AppendCallback(Action)` | 追加回调 |
| `Insert(float, Tween)` | 在指定位置插入 |
| `Play()` / `Pause()` / `Resume()` / `Kill()` / `Complete()` | 控制 |
| `OnComplete` | 完成回调 |

---

## 兼容性

- Unity 2020.3 LTS 及以上
- 无第三方依赖
